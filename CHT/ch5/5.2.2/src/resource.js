var res = {
    background_png : "res/background.png",
    SpirteSheet_png : "res/SpirteSheet.png",
    SpirteSheet_plist :  "res/SpirteSheet.plist"
};

var g_resources = [
    //image
    //plist

    //fnt

    //tmx

    //bgm

    //effect
];

for (var i in res) {
	g_resources.push(res[i]);
}
