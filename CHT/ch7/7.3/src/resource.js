var res = {
		run_png : "res/run.png",
		run_plist : "res/run.plist",
		Background_png : "res/background.png"
};

var g_resources = [
    //image
    //plist
    //fnt
    //tmx
    //bgm
    //effect
];

for (var i in res) {
	g_resources.push(res[i]);
}